// resources/js/Hooks/useStickFormActions.ts
import { useReducer } from "react";

const useStickFormActions = () => {
  /*const [state, dispatch] = useReducer(reducer, initialState);

  const checkSimilarProducts = async (data: TRawStep1Data) => {
    try {
      dispatch({ type: 'SET_LOADING', payload: true });
      const validated = validateStep1(data);
      const res = await api.checkSimilar(validated);
      return res;
    } finally {
      dispatch({ type: 'SET_LOADING', payload: false });
    }
  };

  const createProduct = async (data: TRawStep1Data) => {
    const validated = validateStep1(data);
    const res = await api.createProduct(validated);
    return res; // { productId: number }
  };

  return { state, dispatch, checkSimilarProducts, createProduct };*/
};
